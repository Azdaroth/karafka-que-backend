RSpec.describe Karafka::Que::Backend do
  it "has a version number" do
    expect(Karafka::Que::Backend::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
